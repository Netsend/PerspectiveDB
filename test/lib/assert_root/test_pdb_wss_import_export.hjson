# PersDB settings.

# The strategy used for options in default.hjson shipped with PersDB is
# to specify options with their default value where possible, but leave them
# commented. Uncommented options override the default value.

# Main

#user: _pdbnull
#group: _pdbnull
#chroot: /var/empty

log: {
  console: true
  level: debug
}

# Milliseconds between auto processing items
autoProcessInterval: 100

# DBs

dbs: [{
  name: someDb
  chroot: /var/pdb
  path: /test_pdb_root
  mergeTree: {
    vSize: 3,
  },
  perspectives: [
    {
      name: someClient
      passdb: passdb.hjson
      import: true
      export: true
    }
  ]
}]

port: 1234

wss: {
  cert: cert.pem
  key: key.pem
  dhparam: dhparam.pem
  port: 1235
}
